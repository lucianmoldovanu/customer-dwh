resources:
  snowflake:
    config:
      account:
        env: SNOWSQL_ACCT
      user:
        env: SNOWSQL_USER
      password:
        env: SNOWSQL_PWD
      database: CUSTOMER_DWH
      schema: B2B
      warehouse: COMPUTE_WH
  dbt:
    config:
      project_dir: PIPELINE/ops/dbt