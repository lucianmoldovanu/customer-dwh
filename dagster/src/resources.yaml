resources:
  snowflake:
    config:
      account:
        env: SNOWFLAKE_HOST
      user:
        env: SNOWFLAKE_USER
      password:
        env: SNOWFLAKE_PASS
      database: CDWH
      schema: CORE
      warehouse: CDWH_WH
  dbt:
    config:
      project_dir: DBT