resources:
  snowflake:
    config:
      account:
        env: SNOWFLAKE_HOST
      user:
        env: SNOWFLAKE_USER
      password:
        env: SNOWFLAKE_PASSWORD
      database: CUSTOMER_DWH
      schema: B2B
      warehouse: COMPUTE_WH
  dbt:
    config:
      project_dir: DBT